{{ form_start(form, {"attr":{"novalidate":"novalidate"}}) }}
    {{ form_widget(form) }}
    <button class="btn btn-{{ button_color|default('primary') }}">{{ button_label|default('Save') }}</button>
    <a class="btn btn-secondary" href="{{ path('category_index') }}">Cancel</a>
{{ form_end(form) }}
